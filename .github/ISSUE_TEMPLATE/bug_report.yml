name: Bug report
description: Report a reproducible problem in the Linux preview.
title: "[Bug] "
labels:
  - bug
body:
  - type: markdown
    attributes:
      value: |
        Please remove API keys, access tokens, file contents, prompts, chat transcripts, and other private data before posting.
  - type: input
    id: platform
    attributes:
      label: Platform
      description: Include Linux distribution, desktop session, and whether you use X11 or Wayland.
      placeholder: Linux Mint 22, Cinnamon, X11
    validations:
      required: true
  - type: input
    id: provider
    attributes:
      label: Provider
      description: Name the provider, CLI or compatible endpoint, and model if relevant.
      placeholder: Claude Code, Antigravity, LM Studio, or another provider
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: PilferedParrot version
      description: Include the release tag or commit and relevant CLI/browser versions.
      placeholder: v0.5.0; Chrome 140; provider CLI version
    validations:
      required: true
  - type: textarea
    id: reproduction
    attributes:
      label: Steps to reproduce
      description: Keep the steps concise and use placeholders for private paths or data.
      placeholder: |
        1. Open ...
        2. Select ...
        3. Choose ...
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
    validations:
      required: true
  - type: textarea
    id: actual
    attributes:
      label: Actual behavior
    validations:
      required: true
  - type: textarea
    id: diagnostics
    attributes:
      label: Sanitized diagnostics
      description: Optional logs or error text. Remove keys, tokens, prompts, chat data, private paths, and project contents.
      render: text
